@model IEnumerable<TBS.Model.Title>

@{
    ViewBag.Title = "Index";
    
    <p><b>Project and it's scope.</b>  There is lots of scope to implement other patterns such as Unit of Work, Lazy Load, 
                    Dependency Injection, Data Mapper, Entity Framework, Style Sheets, Logo, Images, Login </p>
}
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TitleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TitleNameSortable)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReleaseYear)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProcessedDateTimeUTC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TitleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TitleNameSortable)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ReleaseYear)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProcessedDateTimeUTC)
        </td>
        <td>
            @Html.ActionLink("Details", "Details", new { id=item.TitleId }) 
        </td>
    </tr>
}

</table>
